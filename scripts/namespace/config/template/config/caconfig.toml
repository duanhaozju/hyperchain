# CA & CERT configs
#
# this config prepare  for ca & cert module
# *.toml config file is batter than *.yaml
# this config will never change by hyperchain
# please ensure the file not end with the `/` (slash)
#

[ecert]
ca = "namespaces/template/config/cert/eca.ca"
cert = "namespaces/template/config/cert/ecert.cert"
priv = "namespaces/template/config/cert/ecert.priv"

[rcert]
# if you do not have rcert, leave this item blank
ca = "namespaces/template/config/cert/rca.ca"
cert = "namespaces/template/config/cert/rcert.cert"
priv = "namespaces/template/config/cert/rcert.priv"

# the tls cert supply the transport layer security
[tlscert]
ca = "namespaces/template/config/cert/tlscert/tlsca.ca"
cert = "namespaces/template/config/cert/tlscert/tls_peer1.cert"
priv = "namespaces/template/config/cert/tlscert/tls_peer1.priv"
serverhostoverride = "hyperchain.cn"

[check]
# check the ecert and rcert signature during the connect progress
certsign = true
# check the tcert during the request process
tcert  = true
# check the ercert and rcert
ercert = true
