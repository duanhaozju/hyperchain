contract Demo{
    struct User{
        uint8   id;
        uint8[2]    age;
        string[] desc;
    }
    User user;
    string[] temp;
    User[] users;
    function Demo(){
        temp.push("b");
        temp.push("aaaaaaaaaabbbbbbbbbbccccccccccddddddddddeeeeeeeeeeffffffffffgggggggggg");
        user = User(1,[3,4], temp);
        users.push(User(5,[7,8], temp));
        users.push(User(19,[20,21], temp));
    }
}