#test data for common/config
server:
        # current version of the CA
        version: "0.1"

        # port the CA services are listening on
        port: 50051
        duration: 3s


        tls:
            cert:
                need: false
            key:
                value: 12.34
