syntax = "proto3";
package types;

message Chain {
    bytes version         = 1;
	bytes latestBlockHash = 2;
	bytes parentBlockHash = 3;
	uint64 height         = 4;
	uint64 genesis        = 5;
	uint64 currentTxSum   = 6;
	bytes  extra          = 7;
}